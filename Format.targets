<Project Sdk="Microsoft.NET.Sdk">

  <Target Name="AutoFormat" AfterTargets="BeforeCompile">
    <Exec Command="powershell -NoProfile -ExecutionPolicy RemoteSigned cd $(MSBuildThisFileDirectory); dotnet format $(SolutionDir)$(SolutionFileName)" />
  </Target>

</Project>
