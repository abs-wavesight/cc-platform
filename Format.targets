<Project>

  <Target Name="AutoFormat" AfterTargets="BeforeCompile">
    <Exec Command="powershell -NoProfile -ExecutionPolicy RemoteSigned cd $(MSBuildThisFileDirectory); dotnet format $(SolutionPath)" />
  </Target>

</Project>
